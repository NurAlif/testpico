# PRIVATE: Google AI Studio key. Keep this only on the backend host; never put it
# in browser JavaScript or commit it to Git.
AI_PROVIDER=ollama
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=qwen3.5:9b
OLLAMA_CONTEXT_LENGTH=8192
OLLAMA_REQUEST_TIMEOUT_SECONDS=120
GEMINI_API_KEY=
GEMINI_MODEL=gemini-flash-latest
GEMINI_REQUEST_TIMEOUT_SECONDS=120
API_MODEL_NAME=qwen3.5:9b-maps

# PRIVATE: used only by this backend for Places API (New).
GOOGLE_PLACES_API_KEY=

# OPTIONAL and public by design: returned only for API clients that choose to
# render live Maps Embed previews. The UI falls back to external Maps links when omitted.
# Restrict it to Maps Embed API and your exact website referrers.
GOOGLE_MAPS_EMBED_API_KEY=

# Strongly recommended outside localhost. Open WebUI and the demo page send
# this value in the Authorization: Bearer header.
APP_API_KEY=

# Local SQLite file used to keep server-side chat history. Back it up with the
# rest of your application data; it contains users' messages and replies.
HISTORY_DATABASE_PATH=data/wanderai.sqlite3

# Comma-separated origins, with no trailing slash.
CORS_ORIGINS=http://localhost:8001,http://127.0.0.1:8001,http://localhost:3000
TRUSTED_HOSTS=localhost,127.0.0.1,maps-api

# Backend protection; Google Cloud quotas are an additional, separate control.
RATE_LIMIT_REQUESTS=30
RATE_LIMIT_WINDOW_SECONDS=60
MAX_PLACE_RESULTS=5
GOOGLE_REQUEST_TIMEOUT_SECONDS=25

DEEPSEEK_API_KEY=
DEEPSEEK_MODEL=deepseek-v4-flash
OPENROUTER_API_KEY=
OPENROUTER_MODEL=openrouter/free
FREEROUTER_BASE_URL=http://localhost:18800/v1
FREEROUTER_API_KEY=
FREEROUTER_MODEL=auto
GROQ_API_KEY=
GROQ_MODEL=llama-3.3-70b-versatile

# Optional: base64-encoded 32-byte key for encrypted per-account provider keys.
# Python creates data/provider-encryption.key automatically when unset.
# Cloudflare: set this as a Worker secret before enabling saved provider keys.
PROVIDER_ENCRYPTION_KEY=

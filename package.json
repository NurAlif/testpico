{
  "name": "wanderai-worker",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build:site": "npm run build:ui && node scripts/build-site.mjs",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply wanderai-data --remote",
    "build:ui": "vite build",
    "test:ui": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@vitejs/plugin-vue": "^6.0.8",
    "vite": "^8.2.2",
    "wrangler": "^4.129.0"
  },
  "dependencies": {
    "vue": "^3.5.42"
  }
}

{
  "name": "wanderai-worker",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build:site": "node scripts/build-site.mjs",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply wanderai-data --remote"
  },
  "devDependencies": {
    "wrangler": "^4.129.0"
  }
}
